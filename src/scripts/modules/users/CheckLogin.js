//This function checks session storage for a login token. If it exists, it will display the users page, if it doesn't exist it will display the login/registration page

// checkLoginToken = () => {
//     if (sessionStorage.getItem("loginToken")) {
//         buildUiDom();
//     }
//     else {

//     }
// };